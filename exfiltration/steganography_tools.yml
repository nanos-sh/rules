---
title: steganography_tool_usage
description: Detects execution of known steganography tools that hide data within images or files.
author: nanos.sh
severity: medium
mode: live
detection_mode: scheduled
schedule: "*/10 * * * *"
lookback: 30m
folder: endpoint
mitre_tactics:
  - TA0010
  - TA0005
mitre_techniques:
  - T1027.003
tags:
  - steganography
  - covert
  - exfiltration
ai_triage_hints:
  ignore_when:
    - "Known CTF or security research activity"
  suspicious_when:
    - "Tool used with image files and data together"
    - "Followed by file upload or email"
---
source_type=endpoint_edr OR source_type=windows_sysmon
| where action="process_create"
| where process CONTAINS "steghide" OR process CONTAINS "openstego" OR process CONTAINS "snow.exe" OR process CONTAINS "invisible secrets" OR process CONTAINS "Invoke-PSImage"
| risk score=70 entity=src_host factor="Steganography tool detected"
| where process CONTAINS "embed"
| risk score=85 factor="Data embedding operation"
| table timestamp, src_host, user, process_name, process, risk_score, risk_factors
